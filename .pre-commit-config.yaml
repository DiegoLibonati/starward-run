repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.12
    hooks:
      - id: ruff
        args: [--fix]
        exclude: ^migrations/
      - id: ruff-format
        exclude: ^migrations/